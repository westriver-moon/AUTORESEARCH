# ybj 清理执行回执（2026-08-07）

## 执行依据

- 删除清单：`server-ybj-final-delete-list-20260807.txt`
- 清单 SHA-256：`DE86764E3DF613204EADDD8F683159804A48347E11797574ECB610B5F5A9E846`
- 批准路径数：214,766

执行前，本地与服务器分别验证了清单哈希、行数、唯一性、CRLF 编码、路径白名单和文件类型。服务器依据当前目录重新计算候选集，结果与上传清单逐项一致。允许范围仅包括：

- `/home/cgv841/ybj/non_research/codex_proxy/backups/` 下列入清单的旧备份；
- `/home/cgv841/ybj/autoresearch-v2/__pycache__/` 下列入清单的 `.pyc`；
- `/home/cgv841/ybj/%ln`。

## 执行结果

```text
状态：deleted_and_verified
删除文件：214,766
删除逻辑字节：247,759,846 B
备份文件：214,774 -> 12
Python 缓存：3 -> 0
根目录 %ln：已删除
文件系统可用空间：66,984,415,232 -> 67,863,699,456 B
实际增加可用空间：879,284,224 B（约 838.55 MiB）
```

首次前台执行因 SSH 连接重置而中断；只读复核证明该次中断发生在删除开始之前，全部目标仍存在。随后使用相同固定清单启动可监控的服务器后台任务，任务从 0 个缺失项开始并完成全部删除。

## 保留项验证

- 9 个各 code-server 组的最新备份保留。
- 3 个特殊环境备份保留。
- 12 个保留备份逐文件大小和 SHA-256 校验未变。
- 18 个 `git-archives` 恢复/溯源文件的路径、大小和修改时间校验未变；逻辑大小仍为 1,035,299,422 B。
- 最终清单中的 214,766 个目标独立复核后均不存在。
- 清理后手动运行完整 hook 安装链，备份数保持 `12 -> 12`。
- 再跨过一个分钟级 cron 周期，备份数仍保持 `12 -> 12`。

本次删除为直接解除链接，已删除文件不能从原目录直接恢复；保留备份和 Git 恢复归档未被删除。
