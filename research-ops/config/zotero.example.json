{
  "version": 1,
  "enabled": false,
  "mode": "web_api",
  "library_type": "user",
  "user_id": "",
  "group_id": "",
  "target_collection_key": "",
  "collection_name": "Codex Research Library",
  "api_key_env": "ZOTERO_API_KEY",
  "notes": [
    "Copy this file to zotero.local.json for local overrides.",
    "Do not put API keys in this file.",
    "Set ZOTERO_API_KEY and either ZOTERO_USER_ID or ZOTERO_GROUP_ID in the user environment.",
    "Use local_api mode only when Zotero Desktop is running and you intentionally want read-only local checks."
  ]
}
