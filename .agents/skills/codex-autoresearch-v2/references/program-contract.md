# Program Contract

`autoresearch/program.md` must use YAML front matter.

Required fields:

- `goal`
- `metric`
- `direction`
- `budget_mode`
- `worker_count`

Optional fields:

- `keep_threshold`
- `stop_conditions`
- `mutable_paths`
- `notes`

`direction` must be `higher` or `lower`.
